#!/bin/sh

set -e

# Ensure the correct Ruby version is used
rbenv local 2.7.2

# Install bundler and handle potential errors
if ! gem install bundler; then
  echo "Failed to install bundler"
  exit 1
fi

bundle install
